services:
  application:
    build: .
    environment:
      FLASK_APP: src
      FLASK_ENV: development
      FLASK_RUN_HOST: 0.0.0.0
      FLASK_RUN_PORT: 8080
    ports:
      - 4567:4567
version: "3.6"
